"use client";

import { useEffect, useState } from "react";
import { useParams, useRouter } from "next/navigation";

export default function EngagementDetailPage() { const { id } = useParams(); const router = useRouter(); const [engagement, setEngagement] = useState(null); useEffect(() => { const token = sessionStorage.getItem("asterline_access_token"); if (!token) return router.replace("/portal/login"); fetch(`/api/engagements/${id}`, { headers:{Authorization:`Bearer ${token}`} }).then((response) => response.ok ? response.json() : null).then(setEngagement); }, [id, router]); if (!engagement) return <main className="portal-shell"><div className="portal-main"><p className="loading">Loading engagement…</p></div></main>; return <main className="portal-shell"><nav className="portal-nav"><a href="/portal/dashboard">← Portal</a><span>{engagement.status}</span></nav><section className="portal-main"><div className="section-kicker">Engagement</div><h1>{engagement.title}</h1><div className="panel"><h2>Deliverables</h2>{engagement.deliverables?.length ? engagement.deliverables.map((item) => <p key={item.title}>{item.status} — {item.title}</p>) : <p className="loading">Deliverables will appear here as work progresses.</p>}</div></section></main>; }
